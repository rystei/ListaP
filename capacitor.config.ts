import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LISTA_COMPRAS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
